{
  "name": "pai-agentkit",
  "version": "1.0.0",
  "type": "module",
  "main": "src/agentkit.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "typecheck": "tsc --noEmit --strict --target ES2022 --module esnext --moduleResolution bundler --skipLibCheck --types @cloudflare/workers-types",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "agents": "latest"
  },
  "devDependencies": {
    "wrangler@^4.45.0": "latest",
    "@cloudflare/workers-types": "latest",
    "typescript@^5.7.0": "latest"
  }
}