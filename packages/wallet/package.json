{
  "name": "@pai/wallet",
  "version": "0.1.0",
  "description": "PAI Agent Kit — Pi wallet agent tools",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "clean": "rm -rf dist"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
